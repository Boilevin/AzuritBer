in my case, in robot.h:

String html_line;
int TextConsoleCounter;
int TextMax;


in my case, in robot.cpp:

html_line ="";
TextConsoleCounter = 0;
TextMax = 20000;


void Robot::ShowMessage(String message) {
  if (html_line.length() >= TextMax) {    
    html_line = "";
  }
  html_line += message;
  html_line += " ";
  if (ConsoleToPfod) {
    Bluetooth.print (message);     
  }
  Serial.print(message);
}
void Robot::ShowMessageln(String message) {
  if (html_line.length() >= TextMax) {    
    html_line = "";
  }
  html_line += message;
  html_line += "  \n";
  if (ConsoleToPfod) {
    Bluetooth.println(message);    
  }
  Serial.println(message);
}

void Robot::ShowMessage(float value) {
  if (html_line.length() >= TextMax) {    
    html_line = "";
  }
  String help = "";
  help.concat(value);
  html_line += help;
  html_line += " ";
  if (ConsoleToPfod) {
    Bluetooth.print (value);
  }
  Serial.print(value);
}
void Robot::ShowMessageln(float value) {
  if (html_line.length() >= TextMax) {    
    html_line = "";
  }
  String help = "";
  help.concat(value);
  html_line += help;
  html_line += " \n";    
  if (ConsoleToPfod) {
    Bluetooth.println(value);    
  }
  Serial.println(value);
}